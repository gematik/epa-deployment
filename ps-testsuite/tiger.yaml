tigerProxy:
  trafficEndpoints: ["http://tiger-proxy:8080"] # admin port of tiger-proxy
  activateRbelParsingFor:
    - epa3-vau
  fileSaveInfo.writeToFile: true
  adminPort: 8124

lib:
  activateWorkflowUi: true
  workflowUiPort: 8123
  startBrowser: false
  trafficVisualization: true
  runTestsOnStart: false # to select & run tests via Workflow UI (default is true)
